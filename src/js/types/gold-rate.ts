export type GoldRateCards = {
    id: number;
    gram:string;
    price: number;
}

export type SubscriptionPlanCards = {
    id: number;
    plan: string;
    price: string;
    offer: string;
    save: string;
    features: string[];
    
}